#include "main.h"

/**
 *reverse_array - print the reverse of an array
 *@a: array of n size
 *@n: size of array
 *Return: void
 *
 */

void reverse_array(int *a, int n)
{
	int i = 0;

	for (i = n; i > 0; i--)
		_putchar(i);
	_putchar('\n');
}
