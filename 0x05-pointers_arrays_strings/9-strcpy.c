#include "main.h"
#include <string.h>
/**
 * _strcpy - copy the string pointed to by src to dest
 * @dest: pointer
 * @src: pointer
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
	int a;

	strcpy(dest, src);
	return (dest);
}
