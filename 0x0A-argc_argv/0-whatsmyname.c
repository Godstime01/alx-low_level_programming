#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: number of command line arguments.
 * @argv: array of argument
 * Return: (0) - SUCCESS
 */

int main(int argc, char *argv[])
{
	printf("./%d", argv[0]);
	return (0);
}
